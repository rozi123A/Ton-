# إصلاحات تمت على المشروع

## 1. إصلاح بطء الذكاء الاصطناعي (`server/_core/llm.ts`)

- **تقليل عدد محاولات إعادة المحاولة (Retries):** من 4 إلى 2
- **تقليل التأخير الأساسي:** من 500ms إلى 300ms
- **تقليل الحد الأقصى للتأخير:** من 30,000ms (30 ثانية) إلى 5,000ms (5 ثوانٍ)

**النتيجة:** لو فيه خطأ في API، الرد يكون أسرع بكثير بدل الانتظار أكثر من دقيقتين.

## 2. تقليل حجم استجابة الذكاء الاصطناعي

- **AIStudio.tsx:** تقليل `maxTokens` من 1,000 إلى 500
- **routers.ts:** إضافة `maxTokens` افتراضي = 500 للـ AI chat

**النتيجة:** الاستجابة أسرع وأقل تكلفة.

## 3. إصلاح لوحة تحكم الأدمن - الإحصائيات (`client/src/pages/Admin.tsx`)

### المشكلة السابقة:
- الإحصائيات كانت تعتمد على `dbStatus.connected` == true عشان تتحمّل
- لو `dbStatus` بطيء أو ما يرجع `connected: true`، كل الإحصائيات تتوقف

### الحل:
- **إزالة الاعتماد على `dbStatus.connected`:** الآن كل الاستعلامات تُحمّل مباشرة
- **إرجاع جميع الإحصائيات في `dbStatus` واحد:** إجمالي المستخدمين + VIP + المتصلين
- **تحسين عرض الأخطاء:** رسالة تحذير غير حجب بدل منع عرض البيانات
- **زيادة retry:** من 1 إلى 2 مع تأخير 1000ms
- **تقليل refetchInterval:** المتصلين الآن يُحدّث كل 60 ثانية

## 4. تحسين دوال قاعدة البيانات (`server/db.ts`)

- **تحسين رسائل الأخطاء:** إضافة `console.warn` عند فقدان الاتصال بدل السكوت
- **تحسين `getOnlineUsersCount`:** زيادة الفترة من 3 إلى 5 دقائق
- **تحسين `updateUserPresence`:** تحديث `lastSeen` أيضاً مع `lastSignedIn`
- **تحسين `dbStatus` router:** إرجاع `totalUsers` + `premiumUsers` + `onlineUsers` في استعلام واحد (Parallel)
